"use client";
import Link from "next/link";
export default function CartSummary({ subtotal, discount, vat, shipping, grandTotal, onCheckout }: { subtotal: number; discount: number; vat: number; shipping: number; grandTotal: number; onCheckout: () => void }) { const money=(v:number)=>`৳${Math.round(v).toLocaleString()}`; return <section className="cart-summary" aria-label="Cart summary"><div><span>Subtotal</span><strong>{money(subtotal)}</strong></div><div><span>Discount</span><strong>-{money(discount)}</strong></div><div><span>Shipping</span><strong>{shipping ? money(shipping) : "Free"}</strong></div><div><span>VAT</span><strong>{money(vat)}</strong></div><div className="total"><span>Total</span><strong>{money(grandTotal)}</strong></div><Link href="/checkout" onClick={onCheckout}>Proceed to Checkout</Link></section>; }
