"use client";
import Image from "next/image";
import Link from "next/link";
import { Minus, Plus, ShoppingCart, Trash2, X } from "lucide-react";
import { useEffect, useRef, useState } from "react";
import { useCart } from "@/context/CartContext";
import styles from "./CartButton.module.css";
export default function CartButton(){const {items,count,subtotal,removeItem,updateQuantity}=useCart();const [open,setOpen]=useState(false);const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const close=(e:MouseEvent)=>{if(ref.current&&!ref.current.contains(e.target as Node))setOpen(false)};document.addEventListener("mousedown",close);return()=>document.removeEventListener("mousedown",close)},[]);return <div className={styles.wrap} ref={ref}><button className={styles.trigger} onClick={()=>setOpen(v=>!v)} aria-expanded={open}><span><ShoppingCart size={22}/><b>{count}</b></span><span><small>Cart</small><strong>৳{subtotal.toFixed(0)}</strong></span></button>{open&&<div className={styles.panel}><header><div><small>Shopping cart</small><strong>{count} item{count===1?"":"s"}</strong></div><button onClick={()=>setOpen(false)}><X size={18}/></button></header><div className={styles.items}>{items.length?items.map(item=><article key={item.id}><div className={styles.image}>{item.image?<Image src={item.image} alt={item.name} fill sizes="56px" unoptimized/>:<ShoppingCart size={20}/>}</div><div className={styles.info}><Link href={`/products/${item.slug}`} onClick={()=>setOpen(false)}>{item.name}</Link><strong>৳{item.price.toFixed(2)}</strong><div className={styles.qty}><button onClick={()=>updateQuantity(item.id,item.quantity-1)}><Minus size={13}/></button><span>{item.quantity}</span><button onClick={()=>updateQuantity(item.id,item.quantity+1)}><Plus size={13}/></button></div></div><button className={styles.remove} onClick={()=>removeItem(item.id)}><Trash2 size={16}/></button></article>):<div className={styles.empty}><ShoppingCart size={34}/><strong>Your cart is empty</strong><span>Add products to see them here.</span></div>}</div>{items.length>0&&<footer><div><span>Subtotal</span><strong>৳{subtotal.toFixed(2)}</strong></div><div><Link href="/cart" onClick={()=>setOpen(false)}>View cart</Link><Link href="/checkout" onClick={()=>setOpen(false)}>Checkout</Link></div></footer>}</div>}</div>}
